/**
 * Cheddar Test Parser file
 *
 * Ches v2
 *
 * This file utilizes most and especially esoteric parts of
 * Cheddar syntax in order to test and catch potential errors
 * within the parser
 */
Foo.bar
1.base(2)
"test"
@test
[1, 2, 3]

let a = 1
let a
let a: b = 1
let a: b

a abc
